{
  "name": "@synthesis/mcp-gateway",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(no lint yet)\"",
    "test": "echo \"(no tests yet)\"",
    "migrate": "node dist/db/migrate.js"
  },
  "dependencies": {
    "@synthesis/contracts": "workspace:*",
    "drizzle-orm": "^0.36.4",
    "fastify": "^5.2.1",
    "nats": "^2.29.1",
    "pg": "^8.13.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.1",
    "typescript": "^5.6.3"
  }
}