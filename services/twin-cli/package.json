{
  "name": "@synthesis/twin-cli",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": {
    "twin-cli": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(no lint yet)\"",
    "test": "echo \"(no tests yet)\""
  },
  "dependencies": {
    "@synthesis/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.6.3"
  }
}
